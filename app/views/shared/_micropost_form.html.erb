<%= bootstrap_form_for(@micropost) do |f| %>
    <div class="field">
        <%= f.text_area :content, placeholder:"新信息..." %>
    </div>
    <%= f.submit "提交", class: "btn btn-large btn-primary" %>
<% end %>